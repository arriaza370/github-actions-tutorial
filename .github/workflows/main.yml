name: Mi Primer Workflow

on:
   push:
     branches:
       -main

jobs:
 ejemplo:
  runs-on: ubuntu-latest 
  steps:
    - name: Mostrar Informacion del Evento
      run: |
       echo "Repositorio: ${{github.repository}}"
       echo "Actor: ${{github.actor}}"
       echo "Actor: ${{github.event_name}}"
       
