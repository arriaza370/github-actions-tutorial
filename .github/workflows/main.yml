name: Mi Primer Workflow

on:
   push:
     branches:
       -main

jobs:
 ejemplo:
  runs-on: ubuntu-latest
  env:
     PLATAFORMA: "AWS"
     
  steps:
    - name: Mostrar Plataforma solo si es AWS
      if: env.PLATAFORMA == ' AWS '
      run: echo "La plataforma es AWS"
       
